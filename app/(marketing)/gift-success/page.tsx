import type { Metadata } from 'next';
import GiftSuccessPage from './gift-success-client';

export const metadata: Metadata = {
  title: 'Gift purchased — Sprout Track',
  description: 'Your Sprout Track gift code is on its way.',
};

export default GiftSuccessPage;
